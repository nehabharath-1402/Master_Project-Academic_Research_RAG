[35] processes each retrieved paragraph and its title along- side the query through distinct encoders, then amalgamates the resulting latent representations for decoding by a single decoder to produce the final output. RETRO [36] retrieves rel- evant information for each segmented sub-query, then applies a novel module termed Chunked Cross-Attention (CCA) to integrate the retrieved contents with each sub-query tokens. In addition, there are other noteworthy novel structures within the scope of