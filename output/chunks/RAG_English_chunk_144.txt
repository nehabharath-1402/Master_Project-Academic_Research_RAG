[98] retrieve similar codes using BM25 and generate summary using LSTM. They sep- arately encode the input, the retrieved code, and the corre- sponding summary, then combine the hidden states or logits in the decoder. HGNN [230] instead uses code edit distance for retrieval, and substitutes the code encoder with hybrid GNN on their Code Property Graphs (CPG) [231]. RACE [102] employs separate encoders for the input code difference, the retrieved code differences through dense retrieval, and