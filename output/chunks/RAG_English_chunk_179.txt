in the field of artificial intelligence and machine learning. The retrieval process can not only help yield high-quality images even for rare or unseen subjects, but also reduces the parameter count and computational expense [45], [95], [103]– [107], [303]. For GAN-based model, RetrieveGAN [45] uses a differentiable retriever for image patch selection, facilitating end-to-end training. IC-GAN [95] models data as conditional distributions around each training instance, conditioning both the